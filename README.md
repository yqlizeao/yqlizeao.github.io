# 胶卷的缤纷世界

站点已切换到 [jekyll-theme-chirpy](https://github.com/cotes2020/jekyll-theme-chirpy) 主题，仅保留 `_posts` 文章与 `img/` 资源，其余结构按主题推荐的方式重建。

## 本地开发

```bash
bundle install
bundle exec jekyll serve
```

启动后访问 <http://localhost:4000> 查看效果。
